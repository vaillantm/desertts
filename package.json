{
  "name": "desserts-shop",
  "version": "1.0.0",
  "description": "TypeScript Shopping Cart Application",
  "type": "module",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "cp data.json dist/data.json",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
